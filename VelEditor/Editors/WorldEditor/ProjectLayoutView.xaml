<UserControl x:Class="VelEditor.Editors.ProjectLayoutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VelEditor.Editors"
             xmlns:proj="clr-namespace:VelEditor.GameProject"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=proj:Project}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Button Content="Add Scene" Width="96" VerticalAlignment="Center"
                Focusable="False" Grid.Row="0" HorizontalAlignment="Left"
                Margin="20,0,0,0"
                Command="{Binding  AddSceneCommand}"/>
        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding ScenesList}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Expander  IsExpanded="{Binding IsActive, Mode=OneWay}">
                            <Expander.Header>
                                <Grid>
                                    <StackPanel Orientation="Horizontal">
                                        <Grid VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Name}" Style="{StaticResource LightTextBlockStyle}"/>
                                            <TextBox x:Name="renameTextBox" Text="{Binding Name, UpdateSourceTrigger=Explicit}"
                                                     Style="{StaticResource TextBoxStyle_WithRename}" Tag="{Binding RenameCommand}" Visibility="Collapsed"/>
                                        </Grid>
                                        <Button Content="Remove"
                                                Command="{Binding DataContext.RemoveSceneCommand,
                                            RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding }" Margin="20,0"
                                                Focusable="False"/>
                                        <Button Content="Rename" Click="OnRenameScene_Button_Click" Tag="{Binding ElementName=renameTextBox}"/>
                                        <Button Content="+"
                                                IsEnabled="{Binding IsActive}"
                                                Click="OnAddGameEntity_Button_Click" Margin="5,0"/>
                                    </StackPanel>
                                </Grid>
                            </Expander.Header>
                            <ListBox Style="{StaticResource ListBoxNoScrollStyle}" IsEnabled="{Binding IsActive}" ItemsSource="{Binding GameEntityList}"
                                     SelectionMode="Extended" AllowDrop="True" MinHeight="48" HorizontalContentAlignment="Stretch"
                                     SelectionChanged="OnGameEntities_ListBox_SelectionChanged" Loaded="OnGameEntities_ListBox_Loaded"
                                     KeyDown="OnGameEntities_ListBox_KeyDown" Drop="OnGameEntities_ListBox_Drop">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Background="Transparent" MinHeight="24">
                                            <Grid>
                                                <TextBlock Text="{Binding Name}" Margin="4,0" FontWeight="Medium" VerticalAlignment="Center"/>
                                                <TextBox Text="{Binding Name, UpdateSourceTrigger=Explicit}" Style="{StaticResource TextBoxStyle_WithRename}"
                                                         Padding="1,0,0,0" Visibility="Collapsed"/>
                                            </Grid>
                                            <Button Style="{StaticResource RemoveButtonStyle}"
                                                    x:Name="removeButton" Visibility="Collapsed" Margin="5,0" VerticalAlignment="Center"
                                                    Command="{Binding ParentScene.RemoveGameEntityCommand}"
                                                    CommandParameter="{Binding}"/>
                                        </Grid>
                                        <DataTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Visibility" TargetName="removeButton" Value="Visible"/>
                                            </Trigger>
                                        </DataTemplate.Triggers>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Expander>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
        
    </Grid>
</UserControl>
